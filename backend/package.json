{
  "name": "plantpal-backend",
  "version": "1.0.0",
  "description": "PlantPal backend API for AI plant identification and care management",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev",
    "db:reset": "node scripts/reset_and_setup.js",
    "db:setup": "echo 'Run the schema.sql file in your Supabase SQL editor'"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",

    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "sharp": "^0.33.0",
    "@google/generative-ai": "^0.2.1"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
